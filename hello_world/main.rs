fn main() {
    // rust indenation is 4 spaces not a tab
    //  ! is calling macro instead of function and they behave differenctly
    println!("Hello, world!");
    // And yes a semicolon is needed
}